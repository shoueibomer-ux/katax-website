{
  "name": "katax-backend",
  "version": "1.0.0",
  "description": "Backend for the KATAX (Kaya for Accounting & Tax Services Inc.) website — serves the site and handles the contact form.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "dotenv": "^16.4.5",
    "nodemailer": "^6.9.14",
    "@supabase/supabase-js": "^2.45.0"
  },
  "engines": {
    "node": ">=18"
  }
}
